{% set classes = [
  'c-node',
  'c-node--type-' ~ node.bundle|clean_class,
] %}

<div class="c-node__content-header">

  {% if content.field_surname|field_value %}
    {{ content.field_surname }}
  {% endif %}

  {% if content.field_given_name|field_value %}
    {{ content.field_given_name }}
  {% endif %}

  {% if content.field_job_title|field_value %}
    {{ content.field_job_title }}
  {% endif %}

  {% if content.field_headshot|field_value %}
    {{ content.field_headshot }}
  {% endif %}

  {% if content.field_role_type|field_value %}
    {{ content.field_role_type }}
  {% endif %}

  {% if content.field_twitter|field_value %}
    {{ content.field_twitter }}
  {% endif %}

  {% if content.field_board_member_type|field_value %}
    {{ content.field_board_member_type }}
  {% endif %}

  {# TODO Check the output #}

</div>
<article{{ attributes.addClass(classes) }}>

  {% if content.field_additional_profiles|field_value %}
    {{ content.field_additional_profiles }}
  {% endif %}

  {% if content.field_full_bio|field_value %}
    {{ content.field_full_bio }}
  {% endif %}

  {% if content.field_page_builder|field_value %}
    {{ content.field_page_builder }}
  {% endif %}

  {% if content.field_country|field_value %}
    {{ content.field_country }}
  {% endif %}

  {% if content.field_focus_areas|field_value %}
    {{ content.field_focus_areas }}
  {% endif %}

  {% if content.field_region|field_value %}
    {{ content.field_region }}
  {% endif %}

  {% if content.field_expiration_date|field_value %}
    {{ content.field_expiration_date }}
  {% endif %}

  {% if content.field_short_bio|field_value %}
    {{ content.field_short_bio }}
  {% endif %}

  {% if all_meta is not empty %}
    this is a test
    <section class="c-meta o-section o-section--meta {{ meta_classes }}">
      <dl>
        {% for meta_row in all_meta %}
          {% if meta_row.values is not empty %}
            <div class="c-meta__row o-container">
              <dt class="c-meta__label">{{ meta_row.label }}</dt>

              <dd class="c-meta__values">
                {% for value in meta_row.values %}
                  {% if value.url is not empty %}
                    {% set link_icon = '' %}

                    {% for ext in ['.pdf','.doc','.docx','.txt','.rtf','.odt','.ppt','.pptx','.odp','.xls','.xlsx','.ods'] %}
                      {% if value.url ends with ext %}
                        {% set link_icon = 'download' %}
                      {% endif %}
                    {% endfor %}

                    {% if link_icon is empty and value.target == '_blank' %}
                      {% set link_icon = 'external' %}
                    {% endif %}

                    <a class="o-meta-link" href="{{ value.url }}" {% if value.url is not empty %}target="{{ value.term_target }}{% endif %}">
                      <span class="o-meta-link__text">{{ value.text }}</span>
                    </a>
                  {% else %}
                    <span class="c-meta__value"><span class="c-meta__value-text">{{ value.text }}</span></span>
                  {% endif %}
                {% endfor %}
              </dd>
            </div>
          {% endif %}
        {% endfor %}
      </dl>
    </section>
  {% endif %}
</article>
